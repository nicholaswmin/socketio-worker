{
  "name": "socketio-worker-compare",
  "private": true,
  "type": "module",
  "description": "Comparative browser harness: facade vs real io. Measures the MAIN-THREAD cost and reconnect-event timing the worker bridge ADDS over a direct socket.io client, by running identical workloads through both. Not part of `npm test`.",
  "scripts": {
    "load": "node load.mjs",
    "storm": "node storm.mjs"
  },
  "dependencies": {
    "socket.io": "4.8.3",
    "playwright": "1.60.0"
  }
}
